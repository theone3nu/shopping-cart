export interface CartItem {
    title: string;
    id: number;
    quantity: number;
}
export interface ProductItemProps {
    id: number;
    title: string;
    price: number;
    thumbnail: string;
}
